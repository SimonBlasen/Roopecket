using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class GarageMoneyManager : MonoBehaviour
{
    [SerializeField]
    private GameObject prefabMoneyBill;
    [SerializeField]
    private Transform moneySpawn;
    [SerializeField]
    private TextMeshPro moneyAmountText;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
